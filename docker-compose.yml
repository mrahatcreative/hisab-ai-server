services:
  hisab-ai:
    build: .
    container_name: hisab-ai
    ports:
      - "5328:5328"
      - "8080:8080"
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
    volumes:
      - model-cache:/models
    restart: unless-stopped

volumes:
  model-cache:
